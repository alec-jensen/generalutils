# GeneralUtils config

# You can edit these values

back-enabled: true
# Allows usage of /back command.

kickall-exempt-enabled: true
# Disable kickall exemption

messages:
  permission-message: 'You don''t have permission to use this!'
  # Message that shows when player has insufficient permissions

  kickall-message: 'Kickall command was used!'
  # Kick reason for kickall usage
